{
  "name": "gemma-avatar",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun --hot ./index.ts",
    "start": "bun ./index.ts",
    "build": "rm -rf ./dist && bun build ./index.html --outdir ./dist && cp -R ./public/. ./dist/"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@vercel/node": "^3.0.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@met4citizen/talkinghead": "^1.7.0",
    "three": "~0.180.0"
  }
}
